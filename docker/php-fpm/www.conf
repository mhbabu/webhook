[www]
user = www-data
group = www-data

# ✅ Listen on all interfaces
listen = 0.0.0.0:9000
listen.allowed_clients = 0.0.0.0

pm = dynamic
pm.max_children = 10
pm.start_servers = 2
pm.min_spare_servers = 1
pm.max_spare_servers = 3
